function Out = FiniteD( x,y )
%FINITED Summary of this function goes here
%   Detailed explanation goes here

Kappat=n*0;
for i=1:N
    k1=max(1,i-SD);
    k2=min(N,i+SD);
    Kappat(i)=-(EF(k2)-EF(k1))/(V(k2)-V(k1));
end

end

